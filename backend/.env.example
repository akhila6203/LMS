PORT=5000

# MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=lms

JWT_SECRET=change-me-to-a-long-random-string

# Google OAuth — same Web client ID as frontend VITE_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Optional: extra frontend origins for CORS (comma-separated)
# FRONTEND_URL=http://localhost:8080,http://127.0.0.1:8080
